/**
 * Vercel entry: default-exported Express app (see https://vercel.com/guides/using-express-with-vercel).
 * Local dev: use `npm run dev` / `node server/index.js`.
 */
import app from './server/app.js';

export default app;
